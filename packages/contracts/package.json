{
    "name": "@irs/contracts",
    "version": "1.0.0",
    "private": true,
    "description": "Shared API, event, webhook, and boundary contracts for the Integration Reliability System.",
    "type": "module",
    "scripts": {
      "build": "tsc -p tsconfig.json",
      "typecheck": "tsc -p tsconfig.json --noEmit"
    }
  }